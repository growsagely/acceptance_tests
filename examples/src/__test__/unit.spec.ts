import { add } from "../math";

describe("math", () => {
  test("If Plato had two forms and Socrates gave him three more", () => {
    expect(add(2, 3)).toBe(5); // How many forms does Plato have now in his ideal realm?
  });
});
